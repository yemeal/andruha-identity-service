[project]
name = "andruha-identity-service"
version = "0.1.0"
description = "Credentials and authentication session boundary skeleton"
readme = "README.md"
requires-python = ">=3.14,<4.0"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
